# Environment files
.env
.env.*
!.env.example

# Python
.venv/
.uv-cache/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Node / Next.js
node_modules/
.next/
out/
dist/
pnpm-debug.log*

# Docker / OS / editor
.DS_Store
.idea/
.vscode/

# TypeScript
*.tsbuildinfo

# Local Claude Code config
.claude/

# Vitest coverage output
frontend/coverage/

# Local interview prep (never publish)
interview-defense.md


# Local teaching/learning process (never publish)
teaching-protocol.md
backend/eval/.last_failures.md

# Codex-generated mirror of .claude/ — local only, never commit (teaching process stays out of the repo).
# AGENTS.md is intentionally tracked: it's a committed pointer to CLAUDE.md (the single rule source).
.agents/
